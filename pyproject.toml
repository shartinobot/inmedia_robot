[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inmedia_robot"
version = "1.0.0"
dependencies = [
    "aiogram==3.4.1",
    "aiohttp==3.9.1",
    "python-dotenv==1.0.1"
]
